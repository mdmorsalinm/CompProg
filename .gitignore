# 1. Ignore EVERYTHING
*

# 2. Exception: Allow Git to look inside folders 
# (If you don't add this, Git will ignore the folder itself and never find the .cpp files inside!)
!*/

# 3. Exception: Keep all .cpp files
!*.cpp

# 4. Exception: Keep the .gitignore file itself (so you can commit this setup)
!.gitignore